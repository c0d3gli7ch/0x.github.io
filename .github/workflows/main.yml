name: CONTEXT
on: push
jobs:
  MY_ACTIONS_ENV:
    runs-on: ubuntu-latest
    steps:
      - name: "MY STEP"
        run: |
          echo "$MY_ACTIONS"
          echo "$MY_ACTOR"
        env:
          $MY_ACTIONS: ${{ github.action }}
          $MY_ACTOR: ${{ github.actor }}
